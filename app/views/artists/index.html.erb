<div id="mainContent" class="container-fluid"> 
  <p style="color: green"><%= notice %></p>

  <h1>Artistas</h1>
  <table>
    <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
    </thead>
    <tbody>
      <% @artists.each do |artist| %>
        <tr>
          <td><%= artist.name %></td>
          <td><%= artist.genre_id %></td>
          <td><%= link_to 'Ver', artist %></td>  
          <td><%= link_to 'Editar', edit_artist_path(artist) %></td> 
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>
  <div>
    <%= link_to "Nuevo artista", new_artist_path, class: "btn btn-secondary" %>
  </div>
  <br>
  <div>
    <%= link_to "Volver a la colección", root_path, class: "btn btn-secondary" %>
  </div>
</div>