<p style="color: green"><%= notice %></p>

<%= render @genre %>

<div>
  <%= link_to "Editar", edit_genre_path(@genre) %> |
  <%= link_to "Regresar", genres_path %>

  <%= button_to "Borrar", @genre, method: :delete %>
</div>
